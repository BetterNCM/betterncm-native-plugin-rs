[workspace]
members = [
    "betterncm-macro",
    "betterncm-loader",
    "betterncm-plugin-example",
    "betterncm-plugin-api",
    "cef-sys",
    "cef",
]

[profile.dev]
opt-level = "s"

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
opt-level = "s"
debug = false
strip = true
